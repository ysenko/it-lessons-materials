# План уроку №58: Практична робота. Проект «Крамниця пригод»

**Тип уроку:** Урок формування та вдосконалення вмінь і навичок.

**Мета:**
- **Навчальна:** перевірити та закріпити вміння учнів використовувати змінні, введення даних (`input()`), арифметичні операції, рядкові методи та оператор розгалуження `if/elif/else` у Python.
- **Розвивальна:** розвивати вміння самостійно аналізувати завдання, обирати потрібний рівень складності та виправляти помилки за допомогою Thonny.
- **Виховна:** виховувати відповідальність, уважність і здатність доводити розпочату роботу до кінця в умовах обмеженого часу.

**Обладнання та матеріали:** комп'ютери з ОС Windows 10/11, Thonny IDE, проєктор/інтерактивна дошка з презентацією.

---

## Хід уроку

### 1. Організаційний етап (2 хв)

- Привітання, перевірка присутніх.
- Нагадування правил безпеки під час роботи за комп'ютером.

---

### 2. Перевірка знань — усне бліц-опитування (5 хв)

Учитель швидко опитує клас (по одному учню на запитання):

1. Якою функцією зчитати число, введене користувачем з клавіатури?
2. Що виведе: `print(type(input("Введи: ")))`?
3. Навіщо писати `int()` перед `input()`?
4. Скільки гілок може мати `if/elif/else`?
5. Що виведе: `print("Привіт"[0])`?
6. Яка функція повертає довжину рядка?

---

### 3. Інструктаж до практичної роботи (5 хв)

Учитель пояснює:

- Сьогодні самостійна практична робота на оцінку. Тема: «Крамниця пригод» — міні-гра у стилі RPG.
- Учні **самостійно обирають рівень** (Рівень 1 / Рівень 2 / Рівень 3).
- Вищий рівень = більше балів, але й складніше завдання.
- Рівні 1 і 2 — шаблон коду з пропусками: треба заповнити `___` правильним кодом.
- Рівень 3 — самостійне написання програми з нуля.
- Усі умови та приклади виводу є на слайдах.
- Час роботи — 20 хвилин.
- Після закінчення — зберегти файл у Thonny з назвою `lesson58.py`.

#### Рівні та бали

| Рівень | Назва | Бали |
|---|---|---|
| Рівень 1 | «Новачок» | 6–7 |
| Рівень 2 | «Авантюрист» | 8–9 |
| Рівень 3 | «Майстер» | 10–12 |

---

### 4. Практична робота (20 хв)

Учні самостійно пишуть програму в Thonny за обраним рівнем.

**На 10-й хвилині** — пауза для гімнастики очей (1 хв).

---

#### Завдання. Рівень 1 — «Новачок» (6–7 балів)

Гравець заходить до магазину та хоче купити **залізний меч** вартістю **50 монет**.

Заповни пропуски `___` у шаблоні так, щоб програма працювала правильно:

```python
# Рівень 1: Крамниця пригод

name = input("Введи ім'я гравця: ")
coins = int(input("Введи кількість монет: "))
price = ___        # ціна залізного меча — 50 монет

if coins ___ price:
    remainder = coins ___ price
    print("Гравець", name, "купив Залізний меч! Залишок:", ___, "монет.")
else:
    deficit = price ___ coins
    print(name + ", не вистачає " + str(___) + " монет. Збирай далі!")
```

**Приклад виводу (монет достатньо):**
```
Введи ім'я гравця: Стів
Введи кількість монет: 80
Гравець Стів купив Залізний меч! Залишок: 30 монет.
```

**Приклад виводу (монет не вистачає):**
```
Введи ім'я гравця: Аліса
Введи кількість монет: 30
Аліса, не вистачає 20 монет. Збирай далі!
```

#### Критерії оцінювання Рівня 1

| # | Критерій |
|---|---|
| 1 | Програма запускається без помилок |
| 2 | Правильно зчитує ім'я та монети через `input()` |
| 3 | Ціна записана у змінну: `price = 50` |
| 4 | Умова `if/else` коректна |
| 5 | Правильний вивід при достатній кількості монет |
| 6 | Правильний вивід при нестачі монет |

- **6 балів** — виконано критерії 1–4 (незначні помилки у тексті виводу допустимі).
- **7 балів** — виконано всі критерії 1–6.

---

#### Завдання. Рівень 2 — «Авантюрист» (8–9 балів)

Гравець обирає зброю з асортименту магазину: **меч** (50 монет), **лук** (70 монет), **щит** (40 монет).

Заповни пропуски `___` у шаблоні:

```python
# Рівень 2: Крамниця пригод

name = input("Введи ім'я гравця: ")
coins = int(input("Введи кількість монет: "))

print("Доступна зброя: меч (50), лук (70), щит (40)")
weapon = input("Зброя (меч/лук/щит): ")

# Визначити ціну обраної зброї
if weapon == "меч":
    price = ___
elif weapon == "___":
    price = ___
elif weapon == "___":
    price = ___
else:
    print("Такого товару немає в магазині!")
    price = -1   # позначаємо: товар не знайдено

# Результат покупки (якщо товар є в магазині)
if price >= 0:
    name_upper = name.___()       # ім'я великими літерами
    name_length = ___(name)        # кількість букв в імені
    if coins ___ price:
        remainder = ___ - ___
        print(f"ГРАВЕЦЬ {name_upper} ({name_length} букв) купив {___}! Залишок: {___} монет.")
    else:
        deficit = ___ - ___
        print(f"Не вистачає {___} монет для {___}.")
```

**Приклад виводу:**
```
Введи ім'я гравця: Олег
Введи кількість монет: 90
Доступна зброя: меч (50), лук (70), щит (40)
Зброя (меч/лук/щит): лук
ГРАВЕЦЬ ОЛЕГ (4 букви) купив лук! Залишок: 20 монет.
```

#### Критерії оцінювання Рівня 2

Включають усі 6 критеріїв Рівня 1, плюс:

| # | Критерій |
|---|---|
| 7 | `elif` для трьох видів зброї з правильними цінами |
| 8 | Вивід імені великими літерами (`.upper()`) |
| 9 | Вивід довжини імені (`len()`) |
| 10 | Обробка невідомої зброї (`else`) |
| 11 | Правильний залишок / дефіцит монет |

- **8 балів** — виконано критерії 1–6 + будь-які 2 з 7–11.
- **9 балів** — виконано всі критерії 1–11.

---

#### Завдання. Рівень 3 — «Майстер» (10–12 балів)

Перед покупкою відбувається **випадкова подія**, а для деяких гравців діє **знижка 10%**.

Написати програму **з нуля** за умовою:

1. Підключити модуль `random`.
2. Запитати ім'я та кількість монет.
3. Згенерувати випадкову подію:
   - Якщо `random.randint(1, 2) == 1` — знайдено скарб: `+random.randint(10, 30)` монет.
   - Інакше — пастка: `-random.randint(5, 20)` монет (монети не можуть бути від'ємними).
4. Вивести повідомлення про подію та оновлену кількість монет.
5. Запитати вибір зброї (меч/лук/щит, ті самі ціни).
6. Знижка 10%, якщо **перша літера імені є голосною** (`"аеіоуєїАЕІОУЄЇ"`) **АБО** монет більше 100.
7. Вивести фінальний результат з використанням **f-рядка**.

> Підказка: `name[0] in "аеіоуєїАЕІОУЄЇ"`

**Приклад виводу:**
```
Введи ім'я гравця: Іван
Введи кількість монет: 85
Подія: Ти знайшов скарб! +18 монет. Тепер: 103 монет.
Доступна зброя: меч (50), лук (70), щит (40)
Зброя (меч/лук/щит): меч
Знижка 10%! Нова ціна меча: 45 монет.
ГРАВЕЦЬ ІВАН (4 букви) купив меч! Залишок: 58 монет.
```

#### Критерії оцінювання Рівня 3

Включають усі 11 критеріїв Рівнів 1 і 2, плюс:

| # | Критерій |
|---|---|
| 12 | `import random` та генерація події через `randint` |
| 13 | Правильна логіка події (скарб / пастка, монети ≥ 0) |
| 14 | Умова знижки `or` + перевірка першої літери (`name[0] in ...`) |
| 15 | Правильний підрахунок знижки та оновленої ціни |
| 16 | Використано f-рядок для виводу результату |

- **10 балів** — виконано критерії 1–11 + будь-які 2 з 12–16.
- **11 балів** — виконано критерії 1–11 + будь-які 4 з 12–16.
- **12 балів** — виконано всі критерії 1–16.

---

### 5. Аналіз результатів та корекція (5 хв)

Учитель показує типові помилки:
- Забули `int()` при зчитуванні монет → `TypeError`.
- `if` без `:` → `SyntaxError`.
- Рядок зброї з великої літери: `"Меч"` замість `"меч"` → умова не спрацьовує; нагадати про `.lower()`.

Якщо є час — один учень демонструє свою програму.

---

### 6. Підсумки та домашнє завдання (3 хв)

**Підсумки:** Сьогодні ми застосували знання змінних, `input()`, арифметики, рядкових методів та `if/elif/else` у власному проєкті.

**Домашнє завдання:** За бажанням — доопрацювати програму до наступного рівня складності або додати ще один товар. Готовий файл `lesson58.py` здати на наступному уроці.
